# CẤU TRÚC APP TỈNH THỨC — bốn mục và flow làm việc

> ⛔ **BẢN NÀY ĐÃ CŨ TỪ 28/08/2026. Đừng dựng gì theo nó.**
> Nó mô tả bộ bốn màn của ngày 26/08 — *Hôm nay · **Của tôi** · Dự án · **ROVA*** — mà cả hai cái tên in
> đậm nay không còn: *Của tôi* đã tan vào *Hôm nay*, *ROVA* đổi tên thành *Bảng đo*, và có thêm màn
> *Việc cố định*. Bộ đang chạy thật là **Hôm nay · Việc cố định · Dự án · Bảng đo**.
>
> **Đọc thay bằng:** `kien-truc-app.html` (ba trục · hai phạm vi · bốn màn, Tracy chốt 28/08) và
> `BAN-GIAO-kien-truc-bon-man.md` (bàn giao thi công).
>
> Giữ lại file này vì Mục 7 — *bộ luật trình bày chín điều* — vẫn còn hiệu lực nguyên vẹn, và vì Mục 5
> giữ lý lẽ gốc của ba luật chống rối.


> Bản khung, Tracy chốt trục 26/08/2026: phương án A · luật phạm vi · timeline về *Của tôi* · bỏ mục *Nhìn lại*.
> Đây là **bản đề xuất**, chưa thi công. Mã trong `public/index.html` chưa chạm.
> Nền: `nghien-cuu-quan-ly-du-an.md` (mảng dự án) · `bai-dao-tao-nguyen-ly-cam-ket-va-task.md` (tầng cam kết) · `BAN-GIAO-viec-len-khung-gio.md` (timeline).

---

## 1. Một câu chốt

**Thanh tab xếp theo tầm nhìn xa dần — từ *bây giờ* ra *cả hành trình* — còn chuỗi dự án → cam kết → việc → deep work hiện ra bằng ĐƯỜNG BẤM VÀO BÊN TRONG, không phải bằng mỗi cấp một nút.**

Thanh tab trả lời câu hỏi *"tôi mở app để làm gì"*. Chuỗi bốn cấp là sơ đồ dữ liệu. Lấy sơ đồ dữ liệu làm thanh tab chính là lý do bản cũ rối: sáu nút xếp theo bốn trục trộn nhau — thời gian (Hôm nay) · đơn vị lời hứa (Cam kết, Dự án) · loại việc (Cố định) · cách làm (Deep work) · phạm vi người (ROVA).

**Luật phạm vi — thứ làm cấu trúc này đứng vững:** phạm vi do **nơi đang đứng** quyết định, không do một cái nút chuyển chế độ. Hai mục đầu là của tôi, hai mục sau là của cả đội. Hệ quả gọn nhất: nút *"Cả ROVA"* ở màn cam kết không còn lý do tồn tại.

---

## 2. Bốn mục

| # | Mục | Câu hỏi nó trả lời | Phạm vi | Bên trong |
|---|---|---|---|---|
| 1 | 🌅 **Hôm nay** | *Bây giờ tôi làm gì?* | chỉ tôi | Ba cam kết đang chạy (bản rút gọn) · việc cố định của hôm nay · việc hôm nay · Chờ bạn · Nghi thức hoàn tất |
| 2 | 🌱 **Của tôi** | *Tôi đang hứa gì, ôm gì, xếp vào đâu?* | chỉ tôi | Cam kết của tôi · **Lịch trình (timeline)** · Kho việc · Việc cố định theo tuần · Hành trình deep work |
| 3 | 🧭 **Dự án** | *Chúng ta đang đi đâu, ai gánh cuối?* | **cả ROVA** | Danh mục dự án · hồ sơ từng dự án: số đo · vạch đích · thành viên · milestone kèm cam kết của mọi người · việc của mọi người |
| 4 | 📊 **ROVA** | *Cả đội đang chạy thế nào?* | **cả ROVA** | Kết quả trong ngày · giờ tập trung · giờ deep work của đội · việc cố định của đội |

**Sáu nút xuống còn bốn.** Hai thứ thôi làm *nơi chốn*:

- **Deep work là một hành động** — bấm 💧 trên một việc là vào màn phiên. Cái đáng nhìn của nó (8 ô số, lưới ngày, chuỗi) là một khối trong *Của tôi*, không phải một nút riêng.
- **“Nhìn lại” không tồn tại như một mục.** Khi timeline về đúng chỗ của nó thì mục ấy chỉ còn một khối — một nút cho một khối là thừa nút.

### Bên trong *Của tôi* — năm khối, xếp theo đúng trình tự làm việc

| Thứ tự | Khối | Vai |
|---|---|---|
| ① | **Cam kết của tôi** (bảng đo + ba luống) | Tôi đang hứa gì |
| ② | **🗓 Lịch trình** — ba nấc ngày · tuần · tháng, xem tới trước 60 ngày | Xếp lời hứa ấy vào khung giờ thật. Đây là khối làm việc chính, không gấp |
| ③ | **Kho việc** — tất cả việc của tôi, gồm việc chưa hẹn ngày | Thứ chưa xếp được vào lịch |
| ④ | **Việc cố định theo tuần** — lưới 7 ngày | Nhịp lặp, đứng ngoài chuỗi dự án |
| ⑤ | **Hành trình deep work** — 8 ô số + lưới ngày + chuỗi (gấp lại mặc định) | Tôi đã đi được bao xa |

---

## 3. Flow làm việc — năm cấp, một chiều xuống

```
🧭 DỰ ÁN                muc_tieu (loai = 'du-an')
   │                    có vạch đích · một người gánh cuối · ngày xong
   └─ 🚩 MILESTONE      moc_du_an — 3 tới 7 mốc, mỗi mốc một ngày
        │
        └─ 🌱 CAM KẾT   tieu_diem (muc_tieu_id + moc_id)
             │          lời hứa của MỘT người, gói trong MỘT tuần, có tiêu chí xong
             │
             └─ ✅ VIỆC        task — việc con của cam kết, hoặc việc lẻ trong kho
                  │
                  └─ 💧 PHIÊN DEEP WORK   phien_deepwork — 30 phút = một cái cây
```

**Đọc xuôi (người điều hành):** mở Dự án → bấm một thẻ → thấy milestone nào tới hạn, cam kết nào đang gánh mốc đó, việc nào đang nằm trong kho của ai.

**Đọc ngược (người làm việc):** ở Hôm nay thấy một việc → bấm vào thấy nó thuộc cam kết nào → bấm tiếp thấy cam kết ấy phục vụ dự án nào. ⚠️ *Đường bấm ngược này tôi chưa soi trong mã — cần kiểm trước khi hứa.*

**Chỗ nối duy nhất giữa hai tầng:** một cam kết trỏ về một dự án. Phần trăm tiến độ dự án do máy chủ tự cộng từ các cam kết trỏ về — app không bao giờ hỏi *"bao nhiêu phần trăm rồi"*.

**Lịch trình là nơi chuỗi này chạm mặt đất.** Cam kết cho biết hứa gì, lịch trình cho biết hứa ấy chiếm giờ nào của ngày nào. Nó nhìn cả tới trước, nên nó là công cụ kế hoạch — không phải sổ ghi lại quá khứ.

**Việc cố định (nhịp tuần) đứng ngoài chuỗi** — việc lặp không có vạch đích nên không thuộc dự án nào. Chỗ của nó: khối ④ trong *Của tôi*, và bản của cả đội ở *ROVA*.

---

## 4. Bảng chuyển chỗ

| Khối | Đang ở | Về | Vì sao |
|---|---|---|---|
| 🗓 **Lịch trình** (`tk-timeline`) | tab *Deep work* | 🌱 Của tôi, khối ② | Nó xem tới trước 60 ngày — là chỗ xếp việc vào khung giờ, không phải chỗ nhìn lại |
| Tổng quan deepwork (`tq-*`) | tab *Deep work* | 🌱 Của tôi, khối ⑤ (gấp) | Một khối không đáng một nút |
| Lưới 7 ngày việc cố định | tab *Cố định* (`man-tuan`) | 🌱 Của tôi, khối ④ | Là một loại việc của tôi, không phải một tầm nhìn riêng |
| “Tất cả việc của tôi” | đáy tab *Cam kết* | 🌱 Của tôi, khối ③ | Đúng chỗ sẵn rồi, chỉ đổi tên màn cho khớp |
| Nút **“Cả ROVA”** (`vuonDoiCheDo`) | tab *Cam kết* | **bỏ** | Cam kết của cả đội nay xem ở Dự án. Phạm vi do nơi đứng quyết định |
| Giờ deep work đội · việc cố định đội | tab *ROVA* | giữ nguyên | Đúng vai dashboard tổng thể |
| Màn phiên deep work (`dw`) | mở từ nút 💧 | giữ nguyên | Deep work là hành động, không phải nơi chốn |
| Màn Dọn việc hôm qua (`don`) | cửa tự bật đầu ngày | giữ nguyên | Là nghi thức mở ngày, không phải một mục |

**Đổi tên trong mã cho tên nút bằng tên màn** — hiện lệch ba chỗ: `man-vuon` mang nhãn *Cam kết*, `man-tasklist` mang nhãn *Deep work*, `man-doi` mang nhãn *ROVA*.

| Nhãn mới | Màn hiện tại | Tên mã đề nghị |
|---|---|---|
| Hôm nay | `man-homnay` | giữ |
| Của tôi | `man-vuon` + `man-tuan` + `man-tasklist` gộp vào | `man-toi` |
| Dự án | `man-duan` · `man-duanho` | giữ |
| ROVA | `man-doi` | `man-rova` |

---

## 5. Ba luật giữ cho nó không rối lại

1. **Một khối chỉ có MỘT địa chỉ.** Nơi khác muốn nhắc tới thì hiện bản rút gọn kèm đường bấm về địa chỉ gốc — như Hôm nay hiện ba cam kết đang chạy, còn nơi quản lý chúng là Của tôi.
2. **Phạm vi do nơi đứng quyết định.** Không nút đổi chế độ nào ở hai mục “của tôi”; hai mục “của chúng ta” thì luôn là cả đội.
3. **Tên nút = tên màn = tên trong mã.** Lệch một chỗ là mỗi phiên sau lại mất công dò lại.

---

## 6. Chờ Tracy chốt

| # | Câu hỏi | Mặc định nếu bạn không nói khác |
|---|---|---|
| 1 | Tên mục thứ hai: **“Của tôi”** hay giữ chữ **“Cam kết”**? | *Của tôi* — vì nó chứa cả lịch trình, việc lẻ và việc cố định, không riêng cam kết |
| 2 | *Của tôi* có năm khối. Khối nào **mở sẵn**, khối nào **gấp lại**? | Mở sẵn: cam kết + lịch trình. Gấp: kho việc · việc cố định · hành trình deep work |

---

## 7. Bộ luật trình bày — chống màn dài lê thê

Tracy chê bản hồ sơ dự án 26/08: *"nhìn bảng nào cũng dài quá"*. Mười hai luật dưới đây rút từ chính
những gì Tracy đã chốt rải rác từ trước, gom về một chỗ để lần sau không phải bắt lại. (Luật 10 và 11
Tracy đặt thêm ngày 29/08, luật 12 ngày 03/09 — cả ba đều đặt sau khi phải chỉ tận nơi một lỗi thuộc
đúng loại ấy.)

| # | Luật | Kiểm bằng gì |
|---|---|---|
| 1 | Thứ quan trọng nhất phải **to nhất và nằm trên**; thông tin nền thì nhỏ, mờ, xuống dưới | Đọc thứ tự khối, cái đáng nhìn nhất có ở màn đầu không |
| 2 | Từ **720px xếp hai cột**, từ **1100px ba cột** — không để một bảng tràn hết bề ngang máy tính | Mở ở 375 · 720 · 1100 · 1440px |
| 3 | Khối dài và khối chỉ để **tra lại** thì mặc định **gấp** | Đếm số khối mở sẵn trên một màn |
| 4 | Thẻ gấp phải **hiện một con số** ở mép phải kể cả lúc thu | Nhìn thanh tiêu đề lúc đang thu |
| 5 | Một con số **không bày ở hai chỗ** trên cùng một màn | Đếm xem ô số nào lặp lại ở tiêu đề khối dưới |
| 6 | **Chú giải cơ chế không nằm thường trực** — giải thích cách máy đếm thì giấu đi | Đếm dòng chữ dạy cơ chế trên màn |
| 7 | Con số bắt người đọc **đi tìm xem là ai** thì thay bằng chính cái tên | "3 thành viên" → "Tracy · Andy · Hafi" |
| 8 | Hai thẻ cùng hàng **cao bằng nhau** — xem khối luật riêng ngay dưới bảng | Nhìn mép dưới hai thẻ cạnh nhau |
| 9 | **Đừng lồng ba lớp hộp** — ruột đã có nền thẻ riêng thì bỏ khung bao ngoài | Đếm số đường viền chồng nhau |
| 10 | **Không gạch chữ.** Việc xong thì **lùi màu**, không gạch ngang chữ và không kẻ ngang thân khối | `grep line-through` phải ra **0**; nhìn một dòng đã xong ở mọi màn |
| 11 | **Khối và bảng không được chạm nhau** — mỗi khối đứng cạnh nhau phải có hở, xem khối luật riêng ngay dưới | Đoạn mã đo hở ở cuối mục này |
| 12 | **Hai lối vào cùng một việc phải dẫn tới CÙNG một cửa** — cùng bộ ô, cùng hành vi, cùng lúc đổi. Thêm một ô ở lối này mà quên lối kia là một tính năng biến mất không tiếng kêu | `thu-hai-cua-dong-bo.js`, và khối luật riêng ngay dưới |

### Luật 12 nói đủ — HAI LỐI VÀO, MỘT CỬA

Tracy 03/09, cầm ảnh cửa mở từ timeline sau khi tính năng «thứ N của tháng» đã lên sóng: *"đi từ cửa
ấn từ timeline nó không hiện này"*, và chốt ngay thành luật: **nút `+ Sự kiện` hay cú chạm timeline
rồi chọn *Sự kiện* đều phải dẫn tới cửa sổ giống nhau và đồng bộ.**

**Vì sao đây là luật chứ không phải một lần sửa lỗi.** Một việc có hai lối vào thì rất tự nhiên sinh
ra hai khung dựng — mỗi lối một chỗ trong mã. Từ đó, mọi lần thêm một ô đều là **hai** việc phải làm,
mà chỉ một việc được nhắc tới trong đầu bài. Quên nửa còn lại thì không có lỗi nào báo: cửa vẫn mở,
vẫn lưu được, chỉ thiếu mất một lối khai — và người phát hiện ra là người dùng, bằng cách không tìm
thấy thứ vừa được hứa.

**Ba việc phải làm mỗi khi chạm một cửa có hai lối vào:**

1. **Sửa cả hai khung, đếm thành hai.** Trong app hiện nay là `lcMoForm` (cửa Sự kiện đầy đủ) và
   `vcVeSuKien` (cửa gộp mở từ timeline). Chúng cố ý mang **cùng tên ô** để `lcLuu` và `lcDoiLap`
   chạy được ở cả hai mà không phải biết mình đang đứng đâu — nhưng khung HTML thì vẫn nằm ở hai chỗ.
2. **Luật hành vi để đúng MỘT chỗ.** Ẩn/hiện, kiểm tra trước khi ghi, gói gửi lên máy chủ — tất cả
   nằm trong hàm dùng chung, không chép vào từng khung. Chép là hai bản sẽ trôi khỏi nhau.
3. **Chỉ MỘT cửa được có mặt trong cây một lúc.** Đây là cái giá của việc dùng chung tên ô:
   `getElementById` trả phần tử **đầu tiên** trong cây, nên khung của cửa vừa đóng mà còn nằm đó thì
   mọi lượt đọc của cửa đang mở đều trỏ nhầm sang nó. Cửa trơ ra, ô không bày, không một tiếng kêu.
   Đóng cửa nào thì **dọn trắng khung của cửa ấy**, đừng chỉ giấu nó đi.

**Kiểm bằng máy, không bằng trí nhớ:** `node production/tinh-thuc-app/thu-hai-cua-dong-bo.js`. Nó
lấy chuỗi HTML thật của cả hai khung, chạy luật ẩn/hiện thật lên từng cái, rồi so từng ô ở từng nhánh
— nó không hỏi *"mã có đúng không"*, nó hỏi *"hai cửa có nói cùng một điều không"*. Bài thử ấy chính
là thứ đã bắt được lỗi ở điểm 3 nằm im từ 02/09.

### Luật 10 nói đủ — VIỆC XONG THÌ LÙI LẠI, KHÔNG BỊ GẠCH BỎ

Tracy chốt lần đầu **08/08** cho dòng việc (*gạch làm chữ khó đọc*), áp tiếp cho bước đã tick, rồi
phải chỉ lần nữa **29/08** vì khối Timeline còn sót: *"task done ở trong timeline ko cần gạch đâu"*.

**Việc đã xong vẫn là việc mình đã làm.** Gạch ngang là ký hiệu của thứ bị **bỏ đi** — nó nói sai về
một thứ đáng ghi công, và nó làm chữ khó đọc đúng lúc người ta muốn xem lại mình đã làm gì.

| Được dùng | Không được dùng |
|---|---|
| **Rút màu về trung tính** — xem cảnh báo ngay dưới bảng | `text-decoration: line-through` |
| **Đổi màu sang `--tt-xong`** — màu nói một mình là đủ | Hạ độ mờ (xem cảnh báo thứ hai ngay dưới) |
| Đổi màu gáy sang `--tt-xong` | Một đường kẻ ngang thân khối (`::after` cao 1px) |
| — | Chồng độ mờ lên một khối đã mang màu xong |
| Ký hiệu tick tròn `.kh.xong` đứng cạnh | Đổi chữ sang màu nền |

⚠️ **ĐỘ MỜ LÀM NHẠT MÀU, KHÔNG LẤY MÀU ĐI.** Cùng ngày 29/08 Tracy bắt tiếp:
*"sao task này done rồi mà vẫn vàng vậy"*. Cơ chế "hạ độ mờ" được đặt ra hồi khối trên lịch còn **tô
đặc** — hạ một mảng navy xuống một nửa thì thấy ngay. Từ lúc khối chuyển sang **nền pha loãng**, thứ
bị hạ đã nhạt sẵn nên `.5` gần như không đổi gì, và mắt vẫn đọc đúng cái màu ấy. **Khối nào nền đã
nhạt thì phải RÚT màu về trung tính, không chỉ hạ độ mờ.** Rút màu không phải gán một màu mới cho
trạng thái — màu nói loại của việc đang sống, việc xong thì không còn loại nào cần phân biệt.

⚠️ **ĐỘ MỜ ĐÃ BỊ BỎ HẲN Ở KHỐI LỊCH (Tracy chốt cuối ngày 29/08:** *"kể ra đã quy định màu thì không
cần làm mờ nữa đâu"***).** Độ mờ đi qua ba đời trên khối việc: tín hiệu DUY NHẤT → tín hiệu thứ hai
chồng lên màu → bỏ. Khi `--mau` của khối đã đổi sang `--tt-xong`, độ mờ không thêm nghĩa nào mà màu
chưa nói, còn cái giá thì có thật: `opacity` hạ **cả khối** nên chữ tên việc mờ theo nền, và một tuần
nhiều việc đã xong đọc thành một lưới bợt màu. Nay ba chỗ cùng một luật — `.tlg-viec.xong` (Tuần ·
Ngày), `.tlt-viec.xong` (Tháng), `.tlg-ca.viec.xong` (dải việc cả ngày, chỗ này trước đây độ mờ là
tín hiệu duy nhất nên phải trao lại tiếng nói cho màu gáy). **Kiểm:** `grep "xong{" ` ba luật ấy phải
KHÔNG còn chữ `opacity`.

Luật này áp cả **ngoài lịch**: `.cl-dong.xong .cl-link` và `.qtp-dong.xong .cl-link` (link trong một
bước đã tick) đổi từ `opacity:.5` sang `color:inherit` — dòng đã tick vốn đã lùi chữ sang `--dim2`,
link chỉ việc thừa hưởng, và vạch chân pha theo `currentColor` nên vẫn đọc ra là bấm được.
⚠️ Ba luật `opacity` còn lại mang tên trạng thái là **MÃ CHẾT**, không dòng nào phát class cho chúng
nữa: `.luong.da-xong` (cả cụm `.luong` đã bị `.bang-ck` thay) · `.bang-ck.xong` · `.don-dong.xong`.
Để nguyên cũng không hại, nhưng **đừng lấy chúng làm mẫu** — soi trước khi chép.

⚠️ **Cái bẫy đã sập một lần.** Khối Timeline giấu `.ten` trên điện thoại, nên vết gạch chữ vô hiệu ở
đó — và cách vá đã chọn là **thêm một vết gạch thứ hai** bằng `::after` thay vì hỏi lại xem có nên
gạch không. Dấu hiệu nào chỉ chạy ở một bề rộng thì thay bằng dấu hiệu chạy ở mọi bề rộng (độ mờ,
màu gáy), đừng chồng thêm một vết nữa.

### Luật 11 nói đủ — HAI KHỐI KHÔNG BAO GIỜ CHẠM NHAU

Tracy **29/08**: *"cho tách phần kho cam kết xuống 1 tí"* · *"phần bảng với đã hoàn thành cũng bị chạm
nhau kìa"*. Cả hai lỗi lộ ra trong cùng một buổi, và đều KHÔNG phải do quên khai khoảng cách — mà do
khoảng cách vốn có bị một thay đổi khác lấy mất.

**Ba nấc hở, mỗi nấc một con số:**

| Nấc | Giữa cái gì | Bao nhiêu | Khai ở đâu |
|---|---|---:|---|
| **Trong một lưới** | Hai thẻ cạnh nhau | **14px** | `gap:14px` |
| **Giữa hai khối** | Đáy một lưới với đỉnh khối sau | **14px** | Rule gom `.cot2, #ds-luong, #vuon-kho-luoi{margin-bottom:14px}` |
| **Mở một mục mới** | Hàng thẻ với dải `.muc-ck` của mục sau | **40px** | `.muc-ck.muc-tach{margin-top:40px}` |

**Vì sao nấc ba phải rộng gần gấp ba.** Mép 22px mặc định của `.muc-ck` đo cho một dải đứng đầu màn
hoặc dưới một thanh chọn. Đứng ngay dưới một hàng **thẻ** thì mắt vừa rời một khối đặc đã gặp ngay
chữ của khối sau — hai mục dính vào nhau thành một.

**⛔ Hai cái bẫy, cả hai đều là khoảng cách BỊ LẤY MẤT chứ không phải bị quên:**

1. **Lưới bỏ margin đáy của con thì CHÍNH NÓ phải mang `margin-bottom` bằng `gap`.** Luật này có sẵn
   trong file từ 10/08 — nhưng nó chỉ liệt kê lưới đang có tên. Dựng lưới mới mà quên thêm tên là hở.
2. **Lưới CŨ vừa mất vỏ cũng phải vào lại rule gom ấy.** `#vuon-kho-luoi` nằm trong một `.the` từ lúc
   dựng, nên phần đệm của cái hộp che giúp nó chỗ hở này suốt nhiều tuần. Bỏ hộp ngày 29/08 là khối
   *Đã hoàn thành* dính ngay vào hàng thẻ kho. **Tháo một cái hộp thì soi lại mọi lưới bên trong nó.**

**Kiểm bằng máy, không bằng mắt** — mở bảng điều khiển trình duyệt và dán:

```js
const man = document.querySelector('.man.hien');
const con = [...man.children].filter(e => getComputedStyle(e).display !== 'none' && e.offsetHeight);
con.slice(0, -1).forEach((e, i) => {
  const ho = Math.round(con[i + 1].getBoundingClientRect().top - e.getBoundingClientRect().bottom);
  if (ho < 12) console.log('❌ chạm nhau', ho + 'px', e.id || e.className, '→', con[i + 1].id || con[i + 1].className);
});
console.log('đã soát', con.length, 'khối của', man.id);
```

### Luật 8 nói đủ — LUẬT VUÔNG MÉP

Tracy chốt lần đầu **10/08**, nhắc lại **17/08** (*"cho các bảng vuông vắn với nhau đi sao cứ để bảng
ngắn bảng dài thế nhỉ"*), và phải nhắc **lần thứ ba ngày 28/08**: *"nguyên tắc các bảng phải bằng
nhau"*. Ba lần cùng một câu là dấu hiệu luật viết chưa đủ chặt — nên viết lại cho hết ý ở đây.

**Ba vế, phải đủ cả ba mới gọi là theo luật:**

| Vế | Nội dung | Viết ra sao |
|---|---|---|
| **1. Mép ngoài bằng nhau** | Mọi thẻ cùng một hàng cao bằng nhau | Lưới để `align-items:stretch` — **đây là mặc định, đừng khai đè** |
| **2. Phần chênh rơi vào RUỘT** | Thẻ ngắn bị kéo cao thì chỗ thừa nằm trong khối nội dung cuối, không đọng thành dải trắng dưới đáy | Thẻ thành `flex-direction:column`, khối nội dung cuối `flex:1` |
| **3. Thẻ cao vượt trội thì đứng VẮT HAI HÀNG** | Một thẻ cao gấp mấy lần thì đừng ép cả hàng cao theo nó | `grid-row:span 2` (án lệ `#rvd-the` ở màn Bảng đo) |

**⛔ Một lý lẽ KHÔNG được nhận:** *"hai thẻ này cao khác hẳn nhau, kéo bằng nhau là bày ra mảng
trống"*. Đúng hai làn ngày 28/08 (AN2 và AN3) đều viện đúng câu ấy để khai `align-items:start` cho
`#man-vcd` và `#hn-hang2`, và Tracy bác cả hai trong cùng một câu. **Mảng trống là triệu chứng của
việc bỏ quên vế 2, không phải lý do để bỏ vế 1.** Thấy mình sắp gõ `align-items:start` cho một hàng
thẻ nội dung thì dừng lại: thứ đang thiếu là `flex:1`, không phải một ngoại lệ.

**Ngoại lệ duy nhất — THẺ GẤP.** Chiều cao của thẻ gấp do người dùng quyết, nên kéo một thẻ đang thu
cao bằng thẻ vừa bung là bày ra đúng khoảng trống vừa được giấu đi. Chỗ đang dùng: `#man-doi >
#the-tq{align-self:start}`. Ngoại lệ này **chỉ áp cho thẻ gấp**, không nới cho thẻ nội dung.

**Kiểm bằng máy, không bằng mắt** — mở bảng điều khiển trình duyệt và dán:

```js
document.querySelectorAll('#hn-hang2, #hn-kho, #man-vcd.hien, #man-doi.hien').forEach(h => {
  const c = [...h.children].filter(e => getComputedStyle(e).display !== 'none' && !e.classList.contains('dau'));
  const cao = c.map(e => Math.round(e.getBoundingClientRect().height));
  console.log(h.id, cao, new Set(cao).size === 1 ? '✅ vuông mép' : '❌ lệch');
});
```

Bản mẫu trước/sau: `mau-vuong-mep-va-muc-cam-ket.html` (đo được 433 · 275 trước, 433 · 433 sau).

---

## 8. Đã thi công những gì (26/08, làn AI)

| Việc | Trạng thái |
|---|---|
| Thanh tab sáu nút còn bốn: Hôm nay · Của tôi · Dự án · ROVA | ✅ |
| `#man-vuon` → `#man-toi`; hai màn `man-tuan` và `man-tasklist` thôi làm màn | ✅ |
| Lịch trình về *Của tôi*, mở sẵn, **nấc mặc định TUẦN** | ✅ |
| Kho việc · Việc cố định · Hành trình deep work thành ba khối gấp | ✅ |
| Lưới việc cố định cả đội chuyển sang *ROVA*, gấp + tải lười | ✅ |
| Bỏ nút "Cả ROVA" ở phần của tôi, thay bằng dải có đúng một lối về | ✅ |
| Lưới màn rộng cho *Của tôi*: 2 cột từ 720px, 3 cột từ 1100px | ✅ |

**Đã đo tại máy** (localhost, bốn bề ngang): 4 nút tab đúng · 10 khối nằm trọn trong màn, không thẻ nào
rơi ra ngoài · ba khối gấp mở được ngay cú bấm đầu · ở 1440px Lịch trình chiếm trọn 1208px và ba khối
gấp thành một hàng ba cột đều 393px · ở 375px xếp dọc gọn.

**Đã qua một vòng thẩm định** (bốn tác tử: tham chiếu chết · luồng người dùng · tải dữ liệu, cộng một
tác tử phản biện thử bác bỏ từng phát hiện). Giữ **13 lỗi thật**, bác 15 báo động giả. Bốn lỗi nặng
cùng một gốc: *trước đây "đổi tab" chính là đường làm mới; gom về một màn thì đường ấy mất.*

| Lỗi | Người dùng gặp gì | Đã chữa |
|---|---|---|
| Bấm dòng "chờ bạn ký nhận" bắn **hai lượt tải chồng nhau** | Khối vừa mở biến mất, kèm câu báo sai "không thấy cam kết đó" | Bật phạm vi cả đội **trước** khi mở màn, còn đúng một lượt |
| Đóng phiên deep work không làm mới lịch trình | Chạy 50 phút xong, dải lịch trước mắt không có phiên vừa làm | `dwTaiLaiMan` gọi đường làm mới chung |
| Năm cửa sửa việc chỉ làm mới **nửa màn** | Kéo việc vào thứ Năm mà bảng trên vẫn xếp nó trong Kho | Dựng `lamMoiCuaToi()`, cả sáu cửa cùng gọi |
| Khối Việc cố định chỉ tải **một lần cho cả phiên** | Số cũ đứng im cả ngày | Mỗi cú bung là một lượt làm mới, có chốt chống bấm kép |

Chín lỗi vừa và nhẹ còn lại cũng đã chữa: lưới cả đội ở ROVA làm mới cùng thẻ trên nó · thẻ gấp thôi bị
kéo cao bằng cả hàng · **năm câu chữ còn chỉ người dùng sang tab đã bỏ** · khoảng đệm đáy thôi rò sang
ba tab kia · khung xương thôi vẽ vào ô đang giấu trong thẻ gấp.

**Còn ngỏ:**
- ⚠️ Hai thẻ ở màn ROVA có tiêu đề gần trùng — *"Việc cố định của cả đội"* (dải giữ nhịp theo người)
  và *"Việc cố định — lưới tuần"* (lưới 12 người × 7 ngày). Nên đặt tên phân biệt hoặc gộp làm một.
- ⚠️ Trạng thái gấp/mở chưa nhớ qua lần tải trang (`THE_THU` là biến trong bộ nhớ). Mặc định là *thu*
  nên tải lại chỉ về trạng thái gọn, không phải trạng thái dài — chấp nhận được, sửa sau nếu vướng.
- ⚠️ Icon tab *Của tôi* vẫn là hình cũ của tab Cam kết.

---

*Dựng 26/08/2026. Bước sau khi bạn gật: bản mẫu HTML thanh tab bấm thử được, rồi mới chuyển khối trong `public/index.html` theo từng làn ghi ở `DANG-LAM.md`.*
